#
# Bolo Michelin's MacOSX:

threads.maximum = 4

# *******************************************************************************************************************
# !!!! Each developer must customize lines 11-12 according to their dev environment
# *******************************************************************************************************************

FLEX_HOME 			= /Developer/Applications/Adobe Flash Builder 4.5/sdks/4.5.1

flexsdk.bin.dir 	= ${FLEX_HOME}/bin
flexsdk.lib.dir 	= ${FLEX_HOME}/frameworks/libs

# Project specific properties

project.root 		= ${basedir}/..
src.dir 			= ${project.root}/src
lib.dir 			= ${project.root}/libs

locale.src 			= ${project.root}/src/l10n/locales
bundle.dir 			= ${basedir}/bundles

deploy.dir 			= /mnt/data/static/babelfx_usingexternalbundles
deploy.assets.dir 	= ${deploy.dir}/
deploy.locale.dir 	= ${deploy.dir}/
deploy.bundle.dir 	= ${deploy.assets.dir}/locales


